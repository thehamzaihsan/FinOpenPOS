# FinOpenPOS - Local Environment Configuration
# The application now uses local SQLite for storage.

# Node.js 22+ builtin SQLite driver will manage data/finopenpos.db
# No external database connection required.

# Next.js API configuration
# (Can be left empty for local development)
